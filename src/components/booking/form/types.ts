export interface BookingDetails {
  firstName: string;
  lastName: string;
  email: string;
  numberOfTickets: number;
}

export interface EventInfo {
  event_date: string;
  location: string;
  address: string;
}